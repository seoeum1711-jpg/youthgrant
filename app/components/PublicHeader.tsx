import { getLatestCrawlRun } from "../../lib/data/d1-repository.ts";

function kst(value:string){return new Intl.DateTimeFormat("ko-KR",{timeZone:"Asia/Seoul",month:"2-digit",day:"2-digit",hour:"2-digit",minute:"2-digit",hour12:false}).format(new Date(value));}
export async function PublicHeader({current="explorer"}:{current?:"explorer"|"saved"}){const run=await getLatestCrawlRun();return <header className="gnb"><div className="gnb-inner"><a className="brand" href="/" aria-label="YouthGrant 홈"><span className="brand-mark" aria-hidden="true"/>YouthGrant</a><nav aria-label="주요 메뉴"><a className={current==="explorer"?"current":undefined} href="/">공고 탐색</a><a className={current==="saved"?"current":undefined} href="/saved">관심 공고</a></nav><p className="updated"><span/>{run?`${kst(run.startedAt)} 갱신`:"수집 실행 전"}</p></div></header>}
