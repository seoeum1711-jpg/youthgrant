import { env } from "cloudflare:workers";
import type { D1DatabaseLike } from "../cloudflare.ts";
import { getSource } from "../collectors/registry.ts";
import { findCrossSourceDuplicateCandidates, summarizePublicDataQuality, type DuplicateCandidate, type DuplicateCandidateRecord, type PublicDataQualitySummary } from "../domain/duplicate-candidates.ts";
import type { DeadlineMode, Opportunity, Verification, ReviewStatus } from "../domain/types.ts";
import { parseSupportTypesJson } from "../domain/support-types.ts";
import { calculateOperationalHealth, summarizeEligibleDiscovery, type DiscoveryStateRow, type OperationalHealth } from "./source-health.ts";

type OpportunityRow={
  id:string;dedupe_key:string;title:string;organization:string;region:"경기"|"서울"|"전국";eligible_region:string|null;field:string|null;facility_types_json:string;support_types_json:string|null;
  application_start:string|null;deadline:string|null;deadline_mode:DeadlineMode;deadline_verification:Verification;deadline_evidence:string|null;deadline_evidence_location:string|null;
  eligibility_verification:Verification;eligibility_evidence:string|null;eligibility_evidence_location:string|null;amount_won:number|null;amount_text:string|null;
  self_burden:string|null;support_details:string|null;review_status:ReviewStatus;published_at:string|null;updated_at:string;
  source_id:string;source_name:string;source_method:"WEB"|"RSS"|"API"|"OPEN_DATA";source_url:string;collected_at:string;
};

export type ReviewQueueItem={id:string;title:string;sourceId:string;sourceName:string;sourceUrl:string;reviewStatus:ReviewStatus;flag:string;reason:string;raw:string;tone:"warning"|"danger";attachmentSummary:string|null};
export type ReviewDetail={opportunity:Opportunity;rawText:string};
export type OpsReviewOverview={candidates:DuplicateCandidate[];quality:PublicDataQualitySummary};
export type SourceMonitorItem={id:string;name:string;method:string;region:string;operationalHealth:OperationalHealth;implemented:boolean;enabled:boolean;lastRunAt:string|null;lastSuccessAt:string|null;consecutiveFailures:number;found:number|null;itemsNew:number|null;itemsUpdated:number|null;matched:number|null;analyzed:number|null;result:string|null;httpStatus:number|null;parserVersion:string|null;errorCode:string|null;error:string|null;attachmentsDiscovered:number|null;attachmentsFetchSuccess:number|null;attachmentsParseSuccess:number|null;attachmentsParseFailed:number|null;attachmentsOcrRequired:number|null;fieldsRecovered:number|null;discoveryPending:number;discoveryComplete:number;discoveryUnsupported:number;discoveryFailed:number};
export type CrawlRunSummary={id:string;status:string;trigger:"MANUAL"|"AUTOMATION";startedAt:string;finishedAt:string|null;errorCount:number};

function db():D1DatabaseLike{if(!env.DB)throw new Error("Cloudflare D1 binding `DB` is unavailable.");return env.DB;}
function facilities(value:string){try{const parsed=JSON.parse(value);return Array.isArray(parsed)&&parsed.every(item=>typeof item==="string")?parsed:["기타 / 확인 필요"];}catch{return["기타 / 확인 필요"];}}
function matchRange(evidence:string|null,facilityTypes:string[]){if(!evidence)return null;for(const facility of facilityTypes){if(facility.includes("확인 필요"))continue;const start=evidence.indexOf(facility);if(start>=0)return{start,end:start+facility.length};}return null;}
function mapOpportunity(row:OpportunityRow):Opportunity{const facilityTypes=facilities(row.facility_types_json);return{id:row.id,dedupeKey:row.dedupe_key,title:row.title,organization:row.organization,sourceId:row.source_id,sourceName:row.source_name,sourceMethod:row.source_method,sourceUrl:row.source_url,region:row.region,eligibleRegion:row.eligible_region,facilityTypes,supportTypes:parseSupportTypesJson(row.support_types_json),field:row.field,applicationStart:row.application_start,deadline:row.deadline,deadlineMode:row.deadline_mode,deadlineVerification:row.deadline_verification,deadlineEvidence:row.deadline_evidence,deadlineEvidenceLocation:row.deadline_evidence_location,eligibilityVerification:row.eligibility_verification,eligibilityEvidence:row.eligibility_evidence,eligibilityEvidenceLocation:row.eligibility_evidence_location,eligibilityMatchRange:matchRange(row.eligibility_evidence,facilityTypes),amountWon:row.amount_won,amountText:row.amount_text,selfBurden:row.self_burden,supportDetails:row.support_details,reviewStatus:row.review_status,collectedAt:row.collected_at};}

const opportunitySelect=`SELECT o.*,s.id AS source_id,s.name AS source_name,s.method AS source_method,rn.url AS source_url,rn.collected_at
  FROM opportunities o
  JOIN opportunity_sources os ON os.opportunity_id=o.id AND os.is_primary=1
  JOIN raw_notices rn ON rn.id=os.raw_notice_id
  JOIN sources s ON s.id=rn.source_id`;

export async function listPublicOpportunities(){const result=await db().prepare(`${opportunitySelect} WHERE rn.relevance_status='IN_SCOPE' AND o.review_status IN ('PUBLISHED','CONFIRMED') ORDER BY CASE WHEN o.deadline_mode='FIXED_DATE' AND o.deadline_verification IN ('VERIFIED','MANUAL_CONFIRMED') THEN 0 WHEN o.deadline_mode='OPEN_ENDED' AND o.deadline_verification IN ('VERIFIED','MANUAL_CONFIRMED') THEN 1 ELSE 2 END,CASE WHEN o.deadline_mode='FIXED_DATE' THEN o.deadline END ASC,o.updated_at DESC LIMIT 200`).all<OpportunityRow>();return result.results.map(mapOpportunity);}
export async function getOpportunity(id:string){const row=await db().prepare(`${opportunitySelect} WHERE o.id=? AND rn.relevance_status='IN_SCOPE' AND o.review_status IN ('PUBLISHED','CONFIRMED') LIMIT 1`).bind(id).first<OpportunityRow>();return row?mapOpportunity(row):null;}
export async function getReviewDetail(id:string):Promise<ReviewDetail|null>{const reviewSelect=opportunitySelect.replace("SELECT o.*","SELECT o.*,rn.raw_text");const row=await db().prepare(`${reviewSelect} WHERE o.id=? AND rn.relevance_status='IN_SCOPE' AND o.review_status IN ('PENDING','REVIEW_REQUIRED','DEFERRED') LIMIT 1`).bind(id).first<OpportunityRow&{raw_text:string}>();return row?{opportunity:mapOpportunity(row),rawText:row.raw_text}:null;}
export async function countManagedSources(){const row=await db().prepare("SELECT COUNT(*) AS count FROM sources").first<{count:number}>();return row?.count??0;}

export async function listReviewQueue():Promise<ReviewQueueItem[]>{
  const reviewSelect=opportunitySelect.replace("SELECT o.*",`SELECT o.*,rn.raw_text,(SELECT COUNT(*) FROM attachments a WHERE a.raw_notice_id=rn.id) AS attachment_count,(SELECT COUNT(*) FROM attachments a WHERE a.raw_notice_id=rn.id AND a.parse_status IN ('PARSED','OCR_REQUIRED','PARSE_FAILED','UNSUPPORTED','HWP_PARSER_BLOCKED')) AS attachment_processed,(SELECT COUNT(*) FROM attachments a WHERE a.raw_notice_id=rn.id AND a.parse_status IN ('PARSE_FAILED','HWP_PARSER_BLOCKED')) AS attachment_failed`);
  const result=await db().prepare(`${reviewSelect} WHERE rn.relevance_status='IN_SCOPE' AND o.review_status IN ('REVIEW_REQUIRED','DEFERRED') ORDER BY o.updated_at DESC LIMIT 100`).all<OpportunityRow&{raw_text:string;attachment_count:number;attachment_processed:number;attachment_failed:number}>();
  return result.results.map(row=>{const eligibility=row.eligibility_verification;const deferred=row.review_status==="DEFERRED";const needsAttachment=eligibility==="NEEDS_ATTACHMENT";const attachmentSummary=row.attachment_count?`첨부 ${row.attachment_count}개 · ${row.attachment_processed}개 분석 · ${row.attachment_failed}개 실패`:null;return{id:row.id,title:row.title,sourceId:row.source_id,sourceName:row.source_name,sourceUrl:row.source_url,reviewStatus:row.review_status,flag:deferred?"보류":needsAttachment?"첨부 확인":"검토 필요",reason:deferred?"운영자가 판단을 보류한 공고입니다.":needsAttachment?"신청자격이 첨부파일에 있어 자동 확정할 수 없습니다.":"신청자격 또는 신청 마감 근거가 충분하지 않아 운영자 검토가 필요합니다.",raw:row.eligibility_evidence??row.raw_text,tone:needsAttachment?"danger":"warning",attachmentSummary};});
}

export async function getOpsReviewOverview():Promise<OpsReviewOverview>{
  const result=await db().prepare(`${opportunitySelect.replace("SELECT o.*","SELECT o.*,rn.relevance_status")} WHERE rn.relevance_status='IN_SCOPE' AND o.review_status<>'EXCLUDED' ORDER BY o.updated_at DESC LIMIT 200`).all<OpportunityRow&{relevance_status:"IN_SCOPE"}>();
  const records:DuplicateCandidateRecord[]=result.results.map(row=>({id:row.id,title:row.title,organization:row.organization,sourceId:row.source_id,sourceName:row.source_name,sourceUrl:row.source_url,publishedAt:row.published_at,collectedAt:row.collected_at,deadline:row.deadline,deadlineVerification:row.deadline_verification,eligibilityVerification:row.eligibility_verification,relevanceStatus:row.relevance_status,reviewStatus:row.review_status}));
  const candidates=findCrossSourceDuplicateCandidates(records);const publicIds=new Set(records.filter(record=>record.reviewStatus==="PUBLISHED"||record.reviewStatus==="CONFIRMED").map(record=>record.id));
  const publicCandidatePairs=candidates.filter(candidate=>publicIds.has(candidate.left.id)&&publicIds.has(candidate.right.id)).length;
  return{candidates,quality:summarizePublicDataQuality(records,publicCandidatePairs)};
}

export async function listSourceMonitor():Promise<SourceMonitorItem[]>{
  const result=await db().prepare(`SELECT s.*,
    sr.started_at AS last_run_at,sr.items_found,sr.items_new,sr.items_updated,sr.items_matched,sr.items_analyzed,sr.result,sr.http_status,sr.parser_version,sr.error_code,sr.error_message,
    sr.attachments_discovered,sr.attachments_fetch_success,sr.attachments_parse_success,sr.attachments_parse_failed,sr.attachments_ocr_required,sr.fields_recovered,
    (SELECT MAX(success.started_at) FROM source_runs success WHERE success.source_id=s.id AND success.status='SUCCESS') AS last_success_at,
    CASE WHEN sr.result='FAILED' THEN (SELECT COUNT(*) FROM source_runs failure WHERE failure.source_id=s.id AND failure.result='FAILED' AND failure.started_at>(SELECT COALESCE(MAX(success.started_at),'') FROM source_runs success WHERE success.source_id=s.id AND success.status='SUCCESS') AND failure.started_at<=sr.started_at) ELSE 0 END AS consecutive_failures
    FROM sources s LEFT JOIN source_runs sr ON sr.id=(SELECT id FROM source_runs latest WHERE latest.source_id=s.id ORDER BY latest.started_at DESC LIMIT 1)
    ORDER BY s.id`).all<Record<string,unknown>>();
  const discoveryRows=await db().prepare(`SELECT rn.source_id,rn.relevance_status,rn.attachment_discovery_status FROM raw_notices rn JOIN source_runs sr ON sr.id=rn.source_run_id WHERE sr.id=(SELECT id FROM source_runs latest WHERE latest.source_id=rn.source_id ORDER BY latest.started_at DESC LIMIT 1)`).all<DiscoveryStateRow>();
  const discovery=summarizeEligibleDiscovery(discoveryRows.results);const numberOrNull=(value:unknown)=>value===null||value===undefined?null:Number(value);return result.results.map(row=>{const id=String(row.id);const definition=getSource(id);const implemented=definition?.implemented??Boolean(row.implemented);const enabled=definition?.enabled??false;const latestResult=row.result?String(row.result):null;const consecutiveFailures=Number(row.consecutive_failures??0);const coverage=discovery.get(id)??{pending:0,complete:0,unsupported:0,failed:0};return{id,name:String(row.name),method:String(row.method),region:String(row.region),operationalHealth:calculateOperationalHealth({implemented,enabled,latestResult,consecutiveFailures}),implemented,enabled,lastRunAt:row.last_run_at?String(row.last_run_at):null,lastSuccessAt:row.last_success_at?String(row.last_success_at):null,consecutiveFailures,found:numberOrNull(row.items_found),itemsNew:numberOrNull(row.items_new),itemsUpdated:numberOrNull(row.items_updated),matched:numberOrNull(row.items_matched),analyzed:numberOrNull(row.items_analyzed),result:latestResult,httpStatus:numberOrNull(row.http_status),parserVersion:row.parser_version?String(row.parser_version):null,errorCode:row.error_code?String(row.error_code):null,error:row.error_message?String(row.error_message):null,attachmentsDiscovered:numberOrNull(row.attachments_discovered),attachmentsFetchSuccess:numberOrNull(row.attachments_fetch_success),attachmentsParseSuccess:numberOrNull(row.attachments_parse_success),attachmentsParseFailed:numberOrNull(row.attachments_parse_failed),attachmentsOcrRequired:numberOrNull(row.attachments_ocr_required),fieldsRecovered:numberOrNull(row.fields_recovered),discoveryPending:coverage.pending,discoveryComplete:coverage.complete,discoveryUnsupported:coverage.unsupported,discoveryFailed:coverage.failed};});
}

export async function getLatestCrawlRun(trigger?:"MANUAL"|"AUTOMATION"):Promise<CrawlRunSummary|null>{const statement=trigger?db().prepare("SELECT id,status,trigger,started_at,finished_at,error_count FROM crawl_runs WHERE trigger=? ORDER BY started_at DESC LIMIT 1").bind(trigger):db().prepare("SELECT id,status,trigger,started_at,finished_at,error_count FROM crawl_runs ORDER BY started_at DESC LIMIT 1");const row=await statement.first<Record<string,unknown>>();return row?{id:String(row.id),status:String(row.status),trigger:row.trigger as "MANUAL"|"AUTOMATION",startedAt:String(row.started_at),finishedAt:row.finished_at?String(row.finished_at):null,errorCount:Number(row.error_count)}:null;}
