import assert from "node:assert/strict";
import test from "node:test";
import { strToU8, zipSync } from "fflate";
import type { D1DatabaseLike } from "../lib/cloudflare.ts";
import { detectAttachmentFormat } from "../lib/attachments/file-type.ts";
import { extractHwpx, extractZipChildren, parseAttachmentBytes } from "../lib/attachments/parsers.ts";
import { applyAttachmentEvidence, extractAttachmentEvidence } from "../lib/attachments/verification.ts";
import { attachmentContentHash, processAttachmentMessage } from "../lib/attachments/pipeline.ts";
import type { ExtractionArtifact } from "../lib/attachments/contracts.ts";
import { discoverAndQueueAttachments, supportsAttachmentDiscovery } from "../lib/attachments/discovery.ts";
import type { AttachmentQueueMessage } from "../lib/attachments/contracts.ts";
import type { RawNotice, SourceDefinition } from "../lib/collectors/contracts.ts";

function pdf(text:string){const escaped=text.replace(/([\\()])/g,"\\$1");const stream=text?`BT /F1 12 Tf 72 720 Td (${escaped}) Tj ET`:"";const objects=["<< /Type /Catalog /Pages 2 0 R >>","<< /Type /Pages /Kids [3 0 R] /Count 1 >>","<< /Type /Page /Parent 2 0 R /MediaBox [0 0 612 792] /Resources << /Font << /F1 5 0 R >> >> /Contents 4 0 R >>",`<< /Length ${stream.length} >>\nstream\n${stream}\nendstream`,"<< /Type /Font /Subtype /Type1 /BaseFont /Helvetica >>"];let output="%PDF-1.4\n";const offsets=[0];objects.forEach((object,index)=>{offsets.push(output.length);output+=`${index+1} 0 obj\n${object}\nendobj\n`;});const xref=output.length;output+=`xref\n0 ${objects.length+1}\n0000000000 65535 f \n${offsets.slice(1).map(offset=>`${String(offset).padStart(10,"0")} 00000 n `).join("\n")}\ntrailer\n<< /Size ${objects.length+1} /Root 1 0 R >>\nstartxref\n${xref}\n%%EOF`;return new TextEncoder().encode(output);}
function hwpx(){const section=`<?xml version="1.0" encoding="UTF-8"?><hp:section xmlns:hp="urn:hancom:section"><hp:p><hp:run><hp:t>신청기간 2026년 9월 1일 09:00 ~ 2026년 9월 18일 18:00까지</hp:t></hp:run></hp:p><hp:tbl><hp:tr><hp:tc><hp:p><hp:run><hp:t>신청대상 서울 소재 청소년수련시설 및 청소년문화의집</hp:t></hp:run></hp:p></hp:tc></hp:tr></hp:tbl></hp:section>`;return zipSync({mimetype:strToU8("application/hwp+zip"),"Contents/section0.xml":strToU8(section)});}
function artifact(role:"PRIMARY"|"FORM",blocks:{location:string;text:string}[]):ExtractionArtifact{return{version:"attachment-v1",attachmentId:"att-1",filename:role==="FORM"?"신청서 양식.hwpx":"모집공고.hwpx",format:"HWPX",role,blocks,metadata:{}};}
function fakeDb(row:Record<string,unknown>){const writes:{query:string;values:unknown[]}[]=[];const db={prepare(query:string){let values:unknown[]=[];return{bind(...next:unknown[]){values=next;return this;},async first(){return row;},async all(){return{results:[]};},async run(){writes.push({query,values});return{meta:{changes:1}};}};},async batch(){return[];}} as unknown as D1DatabaseLike;return{db,writes};}
function discoveryDb(){const writes:{query:string;values:unknown[]}[]=[];const prepare=(query:string)=>{let values:unknown[]=[];return{bind(...next:unknown[]){values=next;return this;},async first(){return{attachment_discovery_status:"PENDING"};},async all(){return{results:[]};},async run(){writes.push({query,values});return{meta:{changes:1}};}};};const db={prepare,async batch(statements:ReturnType<typeof prepare>[]){return await Promise.all(statements.map(statement=>statement.run()));}} as unknown as D1DatabaseLike;return{db,writes};}
const discoverySource=(id:string):SourceDefinition=>({id,name:id,method:"WEB",region:"전국",url:`https://example.com/${id}`,implemented:true,enabled:true,health:"GREEN"});
const discoveryNotice=(url:string):RawNotice=>({sourceId:"source",sourceNoticeId:"1",title:"청소년 기관 지원사업 공고",url,publishedAt:null,rawText:"본문",collectedAt:new Date(0).toISOString(),dedupeKey:"source:1"});

test("MIME and magic bytes reject a PDF-named HTML error page",()=>{const detected=detectAttachmentFormat(new TextEncoder().encode("<html>denied</html>"),"notice.pdf","text/html");assert.equal(detected.format,"UNSUPPORTED");assert.equal(detected.mismatch,true)});
test("KYWA extensionless downloads trust PDF and JPG signatures while rejecting real conflicts",async()=>{
  const pdfBytes=pdf("Application deadline 2026-09-18 18:00");const pdfDetected=detectAttachmentFormat(pdfBytes,"깨진 파일명.pdf","application/unknown; charset=euc-kr");assert.equal(pdfDetected.format,"PDF");assert.equal(pdfDetected.mismatch,false);
  const pdfRow={id:"att-kywa-pdf",raw_notice_id:"raw-1",source_id:"kywa",source_run_id:null,parent_attachment_id:null,name:"깨진 파일명.pdf",url:"https://www.kywa.or.kr/inc/download.jsp?file_no=1",content_type:"application/pdf",extension:"pdf",document_role:"PRIMARY",priority:100,fetch_status:"DISCOVERED",parse_status:"PENDING",content_hash:null,archive_depth:0};const pdfStored=fakeDb(pdfRow);const pdfResult=await processAttachmentMessage({DB:pdfStored.db},{attachmentId:pdfRow.id,rawNoticeId:"raw-1",sourceId:"kywa",url:pdfRow.url},async()=>new Response(pdfBytes,{headers:{"content-type":"application/unknown; charset=euc-kr","content-disposition":"attachment; filename*=UTF-8''kywa-notice.pdf"}}));assert.equal(pdfResult.status,"PARSED");assert.ok(pdfStored.writes.some(write=>write.values.includes("unpdf-1")));
  const jpgBytes=new Uint8Array([0xff,0xd8,0xff,0xe0,0,16,0x4a,0x46,0x49,0x46,0]);const jpgDetected=detectAttachmentFormat(jpgBytes,"깨진 포스터.jpg","application/unknown; charset=euc-kr");assert.equal(jpgDetected.format,"JPG");assert.equal(jpgDetected.mismatch,false);const image=await parseAttachmentBytes({attachmentId:"att-kywa-jpg",filename:"poster.jpg",format:jpgDetected.format,role:"MEDIA",bytes:jpgBytes,archiveDepth:0});assert.equal(image.status,"OCR_REQUIRED");assert.equal(image.parseMethod,"image-metadata-1");
  assert.equal(detectAttachmentFormat(pdfBytes,"notice.pdf","image/jpeg").mismatch,true);
});
test("content hash is stable and changes with bytes",async()=>{assert.equal(await attachmentContentHash(strToU8("same")),await attachmentContentHash(strToU8("same")));assert.notEqual(await attachmentContentHash(strToU8("same")),await attachmentContentHash(strToU8("other")))});
test("HWPX extracts paragraph and table text with section provenance",()=>{const blocks=extractHwpx(hwpx());assert.ok(blocks.some(block=>block.text.includes("2026년 9월 18일")));assert.ok(blocks.some(block=>block.text.includes("청소년문화의집")));assert.ok(blocks.every(block=>block.location.startsWith("section 1")));assert.throws(()=>extractHwpx(zipSync({"random.xml":strToU8("<x/>")})),/HWPX_PACKAGE_INVALID/)});
test("PDF text layer parses while an empty PDF becomes OCR_REQUIRED",async()=>{const normal=await parseAttachmentBytes({attachmentId:"pdf-1",filename:"notice.pdf",format:"PDF",role:"PRIMARY",bytes:pdf("Application deadline 2026-09-18 18:00"),archiveDepth:0});assert.equal(normal.status,"PARSED");assert.ok(normal.artifact?.blocks[0].text.includes("2026-09-18"));const scanned=await parseAttachmentBytes({attachmentId:"pdf-2",filename:"scan.pdf",format:"PDF",role:"PRIMARY",bytes:pdf(""),archiveDepth:0});assert.equal(scanned.status,"OCR_REQUIRED")});
test("ZIP accepts supported children and blocks traversal and nested recursion",async()=>{const safe=zipSync({"docs/notice.pdf":pdf("Deadline")});assert.equal(extractZipChildren(safe).length,1);const malicious=zipSync({"../evil.pdf":pdf("x")});assert.throws(()=>extractZipChildren(malicious),/ZIP_PATH_TRAVERSAL/);const nested=await parseAttachmentBytes({attachmentId:"zip-1",filename:"nested.zip",format:"ZIP",role:"SUPPORTING",bytes:safe,archiveDepth:1});assert.equal(nested.status,"PARSE_FAILED");assert.equal(nested.errorCode,"ZIP_DEPTH_LIMIT")});
test("ZIP children are parsed in memory without a blob-store handoff",async()=>{const bytes=zipSync({"docs/notice.pdf":pdf("Application deadline 2026-09-18")});const row={id:"att-zip",raw_notice_id:"raw-1",source_id:"s",source_run_id:null,parent_attachment_id:null,name:"bundle.zip",url:"https://example.com/bundle.zip",content_type:"application/zip",extension:"zip",document_role:"SUPPORTING",priority:80,fetch_status:"DISCOVERED",parse_status:"PENDING",content_hash:null,archive_depth:0};const stored=fakeDb(row);const result=await processAttachmentMessage({DB:stored.db},{attachmentId:row.id,rawNoticeId:row.raw_notice_id,sourceId:"s",url:row.url},async()=>new Response(bytes,{headers:{"content-type":"application/zip"}}));assert.equal(result.status,"PARSED");assert.ok(stored.writes.some(write=>write.query.includes("parent_attachment_id")));assert.ok(stored.writes.every(write=>!write.query.includes("r2_key")&&!write.query.includes("extraction_r2_key")))});
test("HWP and image failures are explicit non-fatal states",async()=>{const hwp=await parseAttachmentBytes({attachmentId:"hwp-1",filename:"form.hwp",format:"HWP",role:"FORM",bytes:new Uint8Array([0xd0,0xcf]),archiveDepth:0});assert.equal(hwp.status,"HWP_PARSER_BLOCKED");const image=await parseAttachmentBytes({attachmentId:"img-1",filename:"poster.png",format:"PNG",role:"MEDIA",bytes:new Uint8Array([0x89,0x50,0x4e,0x47,0x0d,0x0a,0x1a,0x0a]),archiveDepth:0});assert.equal(image.status,"OCR_REQUIRED")});
test("attachment evidence recovers deadline and eligibility but FORM examples are ignored",()=>{const primary=artifact("PRIMARY",[{location:"section 1 · paragraph 1",text:"신청기간 2026년 9월 1일 09:00 ~ 2026년 9월 18일 18:00까지"},{location:"section 1 · paragraph 2",text:"신청대상 서울 소재 청소년수련시설 및 청소년문화의집"}]);const originalUrl="https://example.com/notice.hwpx";const evidence=extractAttachmentEvidence(primary,originalUrl);assert.equal(evidence.length,2);assert.equal(evidence.find(item=>item.field==="deadline")?.value,"2026-09-18T18:00:00+09:00");assert.deepEqual(evidence.find(item=>item.field==="eligibility")?.value,["청소년수련시설","청소년문화의집"]);assert.equal(evidence[0].originalUrl,originalUrl);assert.equal(extractAttachmentEvidence(artifact("FORM",primary.blocks),originalUrl).length,0)});
test("Samsung Dream verification selects the application close and nonprofit applicant evidence",async()=>{const source=artifact("PRIMARY",[{location:"page 4",text:"신청기간: 2025. 9. 22.(월) ~ 11. 5.(수) 17시\n신청자격 아래 조건을 충족하는 비영리 기관(단체)\n사업설명회 2025. 10. 14.(화) 14:00~16:00"}]);const evidence=extractAttachmentEvidence(source,"https://www.sdream.or.kr/notice.pdf");const deadline=evidence.find(item=>item.field==="deadline");const eligibility=evidence.find(item=>item.field==="eligibility");assert.equal(deadline?.value,"2025-11-05T17:00:00+09:00");assert.match(deadline?.evidenceText??"",/신청기간/);assert.doesNotMatch(deadline?.evidenceText??"",/사업설명회/);assert.deepEqual(eligibility?.value,["청소년 관련 기관·단체"]);assert.match(eligibility?.evidenceText??"",/신청자격[\s\S]*비영리 기관/);const{db,writes}=fakeDb({id:"opp-sdream",deadline:"2025-10-14T23:59:00+09:00",deadline_verification:"VERIFIED",deadline_evidence:"사업설명회 2025.10.14",deadline_evidence_attachment_id:"att-1",eligibility_verification:"UNKNOWN",eligibility_evidence:null,facility_types_json:'["기타 / 확인 필요"]',amount_won:null,amount_text:null,amount_evidence:null,self_burden:null,self_burden_evidence:null,review_status:"REVIEW_REQUIRED"});await applyAttachmentEvidence(db,"raw-sdream",evidence);assert.ok(writes.some(write=>write.values.includes("2025-11-05T17:00:00+09:00")&&write.values.includes("PUBLISHED")));});
test("event and operating dates never become application deadlines",()=>{for(const text of ["행사일 2026년 9월 18일", "사업기간 2026년 9월 1일 ~ 12월 31일", "사업설명회 2026년 9월 10일"]){const evidence=extractAttachmentEvidence(artifact("PRIMARY",[{location:"page 1",text}]),"https://example.com/notice.pdf");assert.equal(evidence.some(item=>item.field==="deadline"),false);}});
test("eligibility requires an application context around the institution",()=>{const introduction=extractAttachmentEvidence(artifact("PRIMARY",[{location:"page 1",text:"주관기관은 비영리 기관이며 센터 소개와 사업 연혁을 안내합니다."}]),"https://example.com/notice.pdf");assert.equal(introduction.some(item=>item.field==="eligibility"),false);const eligible=extractAttachmentEvidence(artifact("PRIMARY",[{location:"page 2",text:"지원대상: 사회복지기관, 학교, 센터 및 청소년수련시설"}]),"https://example.com/notice.pdf");assert.deepEqual(eligible.find(item=>item.field==="eligibility")?.value,["청소년 관련 기관·단체","기타","청소년수련시설"]);});
test("deadline extraction rejects phone-number fragments after a valid date",()=>{const evidence=extractAttachmentEvidence(artifact("PRIMARY",[{location:"page 3",text:"접수기간 : 2026. 8.10.(월) ～ 8.18.(화) 우편접수는 마감일 18:00까지 소인분에 한함 문의 044-202-5314"}]),"https://example.com/notice.pdf");assert.equal(evidence.find(item=>item.field==="deadline")?.value,"2026-08-18T18:00:00+09:00")});
test("institution support amount is separated from total budget and self burden is preserved",()=>{const evidence=extractAttachmentEvidence(artifact("PRIMARY",[{location:"section 2 · paragraph 4",text:"총사업비 5억원이며 선정 기관당 최대 2,000만원 지원, 자부담 없음"}]),"https://example.com/notice.hwpx");assert.equal(evidence.find(item=>item.field==="amount")?.value,20_000_000);assert.equal(evidence.find(item=>item.field==="self_burden")?.value,"없음")});
test("BODY and attachment deadline conflict stays REVIEW_REQUIRED",async()=>{const{db,writes}=fakeDb({id:"opp-1",deadline:"2026-09-17T18:00:00+09:00",deadline_verification:"VERIFIED",deadline_evidence:"본문 접수 마감",eligibility_verification:"UNKNOWN",eligibility_evidence:null,facility_types_json:'["기타 / 확인 필요"]',review_status:"REVIEW_REQUIRED"});const result=await applyAttachmentEvidence(db,"raw-1",[{field:"deadline",value:"2026-09-18T18:00:00+09:00",evidenceText:"접수 마감 2026년 9월 18일 18:00",attachmentId:"att-1",filename:"공고문.pdf",originalUrl:"https://example.com/notice.pdf",location:"page 3",sourceType:"ATTACHMENT"}]);assert.equal(result.conflict,true);assert.ok(writes.some(write=>write.values.includes("REVIEW_REQUIRED")))});
test("an explicit attachment close time refines a date-only deadline on the same day",async()=>{const{db,writes}=fakeDb({id:"opp-1",deadline:"2026-08-18T23:59:00+09:00",deadline_verification:"VERIFIED",deadline_evidence:"접수기간 2026. 8.10. ~ 8.18.",eligibility_verification:"UNKNOWN",eligibility_evidence:null,facility_types_json:'["기타 / 확인 필요"]',amount_won:null,amount_text:null,self_burden:null,review_status:"REVIEW_REQUIRED"});const result=await applyAttachmentEvidence(db,"raw-1",[{field:"deadline",value:"2026-08-18T18:00:00+09:00",evidenceText:"접수기간 2026. 8.10. ~ 8.18. 마감일 18:00까지",attachmentId:"att-pdf",filename:"공고문.pdf",originalUrl:"https://example.com/notice.pdf",location:"page 3",sourceType:"ATTACHMENT"}]);assert.equal(result.conflict,false);assert.ok(writes.some(write=>write.values.includes("2026-08-18T18:00:00+09:00")))});
test("automatic attachment verification preserves manual DEFERRED",async()=>{const{db,writes}=fakeDb({id:"opp-1",deadline:null,deadline_verification:"UNKNOWN",deadline_evidence:null,eligibility_verification:"UNKNOWN",eligibility_evidence:null,facility_types_json:'["기타 / 확인 필요"]',amount_won:null,amount_text:null,amount_evidence:null,self_burden:null,self_burden_evidence:null,review_status:"DEFERRED"});const result=await applyAttachmentEvidence(db,"raw-1",[{field:"deadline",value:"2026-09-18T18:00:00+09:00",evidenceText:"신청마감 2026년 9월 18일 18:00",attachmentId:"att-1",filename:"공고문.pdf",originalUrl:"https://example.com/notice.pdf",location:"page 3",sourceType:"ATTACHMENT"},{field:"eligibility",value:["청소년수련시설"],evidenceText:"신청대상 청소년수련시설",attachmentId:"att-1",filename:"공고문.pdf",originalUrl:"https://example.com/notice.pdf",location:"page 2",sourceType:"ATTACHMENT"}]);assert.deepEqual(result,{recovered:0,verified:0,conflict:false});assert.equal(writes.length,0);});
test("final attachment is not downloaded twice and fetch failure is recorded",async()=>{let fetches=0;const base={id:"att-1",raw_notice_id:"raw-1",source_id:"s",source_run_id:null,parent_attachment_id:null,name:"notice.pdf",url:"https://example.com/notice.pdf",content_type:"application/pdf",extension:"pdf",document_role:"PRIMARY",priority:100,fetch_status:"FETCHED",parse_status:"PARSED",content_hash:"abc",archive_depth:0};const final=fakeDb(base);const skipped=await processAttachmentMessage({DB:final.db},{attachmentId:"att-1",rawNoticeId:"raw-1",sourceId:"s",url:"https://example.com/notice.pdf"},async()=>{fetches++;return new Response()});assert.equal(skipped.status,"SKIPPED");assert.equal(fetches,0);const failed=fakeDb({...base,id:"att-2",parse_status:"PENDING",content_hash:null});await assert.rejects(()=>processAttachmentMessage({DB:failed.db},{attachmentId:"att-2",rawNoticeId:"raw-1",sourceId:"s",url:"https://example.com/fail.pdf"},async()=>new Response("fail",{status:503})),/ATTACHMENT_HTTP_503/);assert.ok(failed.writes.some(write=>write.values.includes("ATTACHMENT_HTTP_503")))});
test("forced reprocessing downloads upstream bytes and never writes a blob",async()=>{const bytes=pdf("Application deadline 2026-09-18 18:00");const row={id:"att-direct",raw_notice_id:"raw-1",source_id:"s",source_run_id:null,parent_attachment_id:null,name:"notice.pdf",url:"https://example.com/notice.pdf",content_type:"application/pdf",extension:"pdf",document_role:"PRIMARY",priority:100,fetch_status:"FETCHED",parse_status:"PARSED",content_hash:"abc",archive_depth:0};const stored=fakeDb(row);let fetches=0;const result=await processAttachmentMessage({DB:stored.db},{attachmentId:row.id,rawNoticeId:row.raw_notice_id,sourceId:"s",url:row.url},async()=>{fetches++;return new Response(bytes,{headers:{"content-type":"application/pdf"}})},true);assert.equal(result.status,"PARSED");assert.equal(fetches,1);assert.ok(stored.writes.every(write=>!write.query.includes("r2_key")&&!write.query.includes("extraction_r2_key")))});
test("unsupported attachment discovery is recorded without fetching",async()=>{const stored=discoveryDb();let fetches=0;const count=await discoverAndQueueAttachments(stored.db,undefined,discoverySource("gggov"),discoveryNotice("https://example.com/notices/1"),"raw-1","run-1",async()=>{fetches++;throw new Error("must not fetch")});assert.equal(count,0);assert.equal(fetches,0);assert.ok(stored.writes.some(write=>write.query.includes("attachment_discovery_status='UNSUPPORTED'")))});
test("supported attachment discovery records COMPLETE with zero or found files",async()=>{for(const html of ["<html>no attachments</html>",'<a href="/files/공고문.pdf">공고문.pdf</a>']){const stored=discoveryDb();const queued:string[]=[];const count=await discoverAndQueueAttachments(stored.db,{async send(message){queued.push(message.url);}},discoverySource("gfgf"),discoveryNotice("https://example.com/view.do?id=1"),"raw-1","run-1",async()=>new Response(html,{headers:{"content-type":"text/html"}}));assert.equal(count,html.includes("pdf")?1:0);assert.equal(queued.length,count);assert.ok(stored.writes.some(write=>write.query.includes("attachment_discovery_status='COMPLETE'")))}});
test("supported attachment discovery records FAILED on fetch error",async()=>{const stored=discoveryDb();await assert.rejects(()=>discoverAndQueueAttachments(stored.db,undefined,discoverySource("gfgf"),discoveryNotice("https://example.com/view.do?id=1"),"raw-1","run-1",async()=>new Response("blocked",{status:403})));assert.ok(stored.writes.some(write=>write.query.includes("attachment_discovery_status='FAILED'")))});
test("KYWA detail discovery supports known file types, dedupes URLs and queues each file",async()=>{const source={...discoverySource("kywa"),url:"https://www.kywa.or.kr/pressinfo/notice_list.jsp"};const notice=discoveryNotice("https://www.kywa.or.kr/pressinfo/notice_view.jsp?no=36664");assert.equal(supportsAttachmentDiscovery(source,notice),true);const html=['<a href="../inc/download.jsp?file_no=1">공고문.pdf</a>','<a href="../inc/download.jsp?file_no=2">신청서.hwpx</a>','<a href="../inc/download.jsp?file_no=3">붙임.hwp</a>','<a href="../inc/download.jsp?file_no=4">자료.zip</a>','<a href="../inc/download.jsp?file_no=1">공고문.pdf</a>'].join("");const stored=discoveryDb();const queued:string[]=[];const count=await discoverAndQueueAttachments(stored.db,{async send(message){queued.push(message.url);}},source,notice,"raw-kywa","run-1",async()=>new Response(html,{headers:{"content-type":"text/html"}}));assert.equal(count,4);assert.equal(new Set(queued).size,4);assert.ok(queued.every(url=>url.startsWith("https://www.kywa.or.kr/inc/download.jsp?file_no=")));assert.ok(stored.writes.some(write=>write.query.includes("attachment_discovery_status='COMPLETE'")))});
test("KYWA completed discovery requeues only recoverable type mismatches on the next natural run",async()=>{const messages:AttachmentQueueMessage[]=[];const db={prepare(query:string){let values:unknown[]=[];return{bind(...next:unknown[]){values=next;return this;},async first(){return{attachment_discovery_status:"COMPLETE"};},async all(){return{results:query.includes("ATTACHMENT_TYPE_MISMATCH")?[{id:"att-failed",url:"https://www.kywa.or.kr/inc/download.jsp?file_no=1"}]:[]};},async run(){return{meta:{changes:0},values};}};},async batch(){return[];}} as unknown as D1DatabaseLike;let fetches=0;const source={...discoverySource("kywa"),url:"https://www.kywa.or.kr/pressinfo/notice_list.jsp"};const count=await discoverAndQueueAttachments(db,{async send(message){messages.push(message);}},source,discoveryNotice("https://www.kywa.or.kr/pressinfo/notice_view.jsp?no=36208"),"raw-kywa","run-1",async()=>{fetches++;throw new Error("must not refetch detail")});assert.equal(count,0);assert.equal(fetches,0);assert.deepEqual(messages,[{attachmentId:"att-failed",rawNoticeId:"raw-kywa",sourceId:"kywa",url:"https://www.kywa.or.kr/inc/download.jsp?file_no=1"}]);});
test("ggyouth detail discovery converts official download functions into queue URLs",async()=>{const source={...discoverySource("ggyouth"),url:"https://ggyouth.or.kr/07_openYard/noticeList.do"};const notice=discoveryNotice("https://ggyouth.or.kr/07_openYard/noticeView.do?idx=261618&atchFileId=FILE_000000000021677");assert.equal(supportsAttachmentDiscovery(source,notice),true);const html=['<a href="javascript:fn_egov_downFile(\'FILE_000000000021677\',\'0\')">모집 공고.pdf</a>','<a href="javascript:fn_egov_downFile(\'FILE_000000000021677\',\'1\')">참가자 모집 안내.hwpx</a>'].join("");const stored=discoveryDb();const queued:string[]=[];const count=await discoverAndQueueAttachments(stored.db,{async send(message){queued.push(message.url);}},source,notice,"raw-ggyouth","run-1",async()=>new Response(html,{headers:{"content-type":"text/html"}}));assert.equal(count,2);assert.deepEqual(queued,["https://ggyouth.or.kr/cmm/fms/FileDown.do?atchFileId=FILE_000000000021677&fileSn=0","https://ggyouth.or.kr/cmm/fms/FileDown.do?atchFileId=FILE_000000000021677&fileSn=1"]);assert.ok(stored.writes.some(write=>write.query.includes("attachment_discovery_status='COMPLETE'")))});
test("sdream discovery converts official onFileDown ids and preserves the new-Opportunity signal",async()=>{const source={...discoverySource("sdream"),url:"https://www.sdream.or.kr/main"};const notice=discoveryNotice("https://www.sdream.or.kr/w/bbs0103V?BBS_SID=BBS25000000000506387");assert.equal(supportsAttachmentDiscovery(source,notice),true);const html='<a href="#null" onclick="OnFileDown(\'FLE25000000000506390\'); return false;">[삼성꿈장학재단] 2026 배움터 교육지원사업 공모요강.pdf</a>';const queued:AttachmentQueueMessage[]=[];const stored=discoveryDb();const count=await discoverAndQueueAttachments(stored.db,{async send(message){queued.push(message);}},source,notice,"raw-sdream","run-1",async()=>new Response(html,{headers:{"content-type":"text/html"}}),true);assert.equal(count,1);assert.deepEqual(queued,[{attachmentId:queued[0].attachmentId,rawNoticeId:"raw-sdream",sourceId:"sdream",url:"https://www.sdream.or.kr/download?fileSid=FLE25000000000506390",notifyOnFinalize:true}])});
test("queue unavailable or send failure keeps existing QUEUE_BLOCKED policy",async()=>{for(const queue of [undefined,{async send(){throw new Error("queue unavailable")}}]){const stored=discoveryDb();await discoverAndQueueAttachments(stored.db,queue,discoverySource("gfgf"),discoveryNotice("https://example.com/view.do?id=1"),"raw-1","run-1",async()=>new Response('<a href="/files/공고문.pdf">공고문.pdf</a>',{headers:{"content-type":"text/html"}}));assert.ok(stored.writes.some(write=>write.query.includes("parse_status='QUEUE_BLOCKED'")))}});
